import { SearchTypeCompanyPipe } from './search-type-company.pipe';

describe('SearchTypeCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTypeCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
