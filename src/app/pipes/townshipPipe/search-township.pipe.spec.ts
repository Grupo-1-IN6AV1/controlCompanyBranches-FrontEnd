import { SearchTownshipPipe } from './search-township.pipe';

describe('SearchTownshipPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTownshipPipe();
    expect(pipe).toBeTruthy();
  });
});
