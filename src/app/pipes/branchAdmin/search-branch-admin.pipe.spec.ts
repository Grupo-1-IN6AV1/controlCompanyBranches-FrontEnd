import { SearchBranchAdminPipe } from './search-branch-admin.pipe';

describe('SearchBranchAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBranchAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
